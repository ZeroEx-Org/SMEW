# scratch .npz written by the demo scripts -- never commit these
_*.npz
*.sweep.npz

# local-only scratch: demo and example scripts, and the dev notebook.
# harness.py itself now lives in smew/ and IS tracked.
case_example.py
check_harness.py
demo_npz.py
demo_open_npz.py
dev.ipynb

# tests/golden/*.npz are NOT ignored -- they are the regression fixtures, and
# freeze_cases.py is tracked because the fixtures cannot be regenerated without it.
!freeze_cases.py
